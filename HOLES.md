# 実験ログ: 仕様の穴

「仕様だけから一意に決まらなかった判断」の記録。分類:

- [A] Normative の欠落・矛盾 → 手を止めて仕様へ戻す(契約修正+版バンプ)
- [B] Web の表現として正当な選択 → 仮置きを記録。仕様は沈黙のままでよい
- [C] 仕様は決めているが、機械(適合テスト)が照合できない → 検査の課題として記録

| # | 部品 | 何が決まっていなかったか | 仮置き | 分類 |
|---|---|---|---|---|
| 1 | Button | start / end スロットと文字の間隔の段。契約は「spacing の inline を引く」とだけ書き、sm / md / lg のどれかを指定していない | `inline-sm` を仮置き → 契約 alpha.5 が inline-md(8px) を明記し解消。実装追従済み | A(解消済み。仕様へ戻す運用の最初の実例) |
| 2 | Icon | アイコンセット未受領のため name → グリフの描画が定義できない | 実装保留(スタブも作らない) | A・既知(iconography.md §6 の TODO と整合。セット受領で解消) |
| 3 | Button | disabled の機構(native 属性 か aria-disabled か)は Web の表現として実装に委ねられている | native `disabled` 属性を選択。根拠: 契約の mirrorsNativeAttr、抑制(state.md §3.2)が無償、支援技術からは閲覧カーソルで到達可能 | B(state.md §5 の3要求は満たす。実測検証は実装 Done の条件) |
| 4 | Button | click イベントの FW 写像 | Svelte 5 の callback prop(`onclick`)に写像 | B(契約: 命名は各 FW の規約に写像される) |
| 5 | StemcellProvider | themes(カスタムテーマ)の色→CSS 変換ユーティリティの置き場所が仕様側で未決 | 実装保留。渡されたら warn を出して無視 | A・既知(StemcellProvider.md §9。tokens 側に置く判断が要る) |
| 6 | Button | pressed の視覚の Web 機構 | `:active` 擬似クラス | B(state.md §3.3 の発火条件の記録と整合) |
| 7 | (harness) | enum を持たない string prop(theme 等)は適合テストが値を照合できない | 名前と既定値のみ照合 | C(契約スキーマの表現力の既知の限界と同根) |
| 8 | Switcher | 閾値評価の実現機構(契約が expressive と明示) | flex-basis 算術(Every Layout 同型)。コンテナクエリは `@container` の条件部が custom property を受けず、prop 駆動の閾値をインスタンスごとに運べないため不採用 | B(切替点の正確さは実測済み。experiments/switcher-threshold/RESULTS.md §1) |
| 9 | Switcher | gap の値語彙(段+大域の原始 X)の不正値時の挙動 | console.warn(無条件。Provider の themes 警告と同じ形)を出し既定の md へ退避 | B(仕様は沈黙のままでよい) |
| 10 | Switcher | threshold の不正値時の挙動。長さでない文字列は calc() を無効にし、flex-basis が auto へ縮退して閾値駆動(Normative)が無警告で内容駆動の折返しに戻る(独立レビューが実測で検出) | 検査(rem の長さか)を行い、不正なら warn して既定 30rem へ退避。当初は構文(CSS の長さか)のみ裁いていたが、単位が rem と裁定され(2026-07。契約 alpha.1。PR #12 で main マージ済み)rem 以外も拒否に更新。数値集合は裁かない(threshold は単一の切替点であり Grid min のような段の選択とは性質が違う。段化は要否未検討だが本 HOLE の対象外) | A(解消済み。無警告消滅は rem 検査で塞ぎ、単位は rem 裁定済み) |
| 11 | Box | 逃げ道(自由 style)の Svelte での形。契約は中立表面(inset)だけを持ち、逃げ道は各実装の土地の声(layout.md §6 の裁定) | `as`(要素の多相)+ `style` + `class` の3口。rest props の全面開放は採らない(開くほど「素の div」に近づき、逃げ道の集約という規範の意味が薄れる)。as は意味的要素の許可リストで裁く: 型だけでは script / style / iframe も通り、危険要素をそのまま生成できた(独立レビューが実測で検出。許可外は warn して div へ退避) | B(消費者が現れて足りなければ広げる) |
| 12 | Box | inset 語彙外の値の退避先。gap 系は既定 md へ退避するが、inset の契約既定は「余白なし」 | warn して余白なし(省略時と同じ)へ退避。存在しない md を発明しない | B |
| 13 | TextField | native の確定 change の遮断(field.md §5 の申し送り: Svelte は light DOM のため構造的保護が無い)。さらに罠が一段深かった: Svelte 5 は change をルートへ委譲するため、バブル側の onchange で stopPropagation しても手遅れで、祖先のリスナーには既に届いている(テストの RED で実証) | 委譲されない capture 側(onchangecapture)で stopPropagation する。契約の change(逐次)は oninput から発火 | B(field.md §5 が予告した取り違えの実装保証。テストが門) |
| 14 | TextField | readonly×invalid が同時に宣言されたときの挙動。仕様は「同時に成立しない」(state.md §6)とだけ定め、宣言されたらどうするかは書いていない | warn して invalid を落とす(aria-invalid を立てず、error 部位も出さない)。Switcher の不正値と同じ warn+退避の形 | B |
| 15 | TextField | value の FW 写像。契約は value + change(逐次)で、uncontrolled は「土地の便宜であり契約外」(field.md §5) | `$bindable` にして bind:value を許す(Svelte の土地の声)。値の所有が実効になる経路は bind: アプリが onchange で差し戻した値は DOM へ流れる(テストで実測)。非 bind + onchange はキーストローク単位の拒否が効かない uncontrolled + 通知(React の defaultValue+onChange 相当)であり、それでも value prop の更新(リセット等)は入力後も DOM へ流れる(テストで実測)。以前ここには「bind しない消費者には controlled + onchange がそのまま成立する」と書いてあった。独立レビューが実測で反証した(非 bind で拒否した文字が DOM に残る)ので訂正する | B(HOLES #4 と同型。React 型の強制同期は bind の存否を検出できない Svelte では bind と両立しない) |
| 16 | TextField | Web の確定タイミング(blur / Enter)の器(web 規範層)を作るか。field.md §8 が「svelte 実装実験で判断する」としていた | 作らずに実装できた。表面は閉じている(rest props 無し)が、light DOM では focusout / keydown が部品の外へバブルするため、確定が欲しいアプリは包む要素で捕捉して組める(#13 と同じ機構の裏面。個別の実測テストは無し)。器は必要の立証待ちを推奨 | B(仕様側 field.md §8 の TODO への報告事項。裁定はオーナー) |
| 17 | TextField | disabled×invalid のときの error 部位の文字の扱い(field.md §8 の未決 TODO)。枠の色は state.md §3.1 が disabled 勝ちと定めるが、error 文字を消すかは未決 | error 文字は出したまま(色も danger.soft-fg のまま)。何が悪いかの情報を disabled が奪う理由は無いと読んだ。枠の disabled 勝ちは CSS の後勝ちで実装し、smoke が回帰保護する。オーナー確認済み(2026-07): このまま進めてよい(実物を見て変えることはありうる) | A・既知(field.md §8 の TODO と整合。暫定で確定) |
| 18 | (仕様側) | tokensRequired の欠落: TextField.css は shape.border-width と focus-ring の幾何(width / style / offset)を使うが、契約の tokensRequired に無い。同じ欠落は Button(shape.border-width)にもあり、Divider / Card は宣言している。適合テストはこれらの存在を検査できない | 仕様へ還元して解消。さらに幾何3点は focusRing:true が暗黙要求する規則に格上げした(裁定 2026-07。GOVERNANCE §6-1・focus-ring.md §4)。契約は幾何を列挙せず引き金 focusRing:true だけを挙げ、spec の check と svelte の conformance 生成器が両方その含意を検査する。shape.border-width は枠を持つ部品が個別に宣言する(還元済み) | A(解消済み。仕様へ戻す運用の実例。独立レビュー minor 指摘が発端) |
| 19 | (harness) | 適合テスト生成器は契約の states / slots / events / a11y.notes を照合しない(props と tokensRequired のみ)。「適合 N」の green は実質より強く聞こえる | 目視レビューで担保。states 等の機械照合は META の拡張が要る(実装側に states の機械可読な表明が無い) | C(#7 と同根。独立レビュー minor 指摘) |
| 20 | Grid | min の値語彙(数値集合・単位)は未確定の seed(layout.md §9)。不正値は grid-template-columns を丸ごと無効にし、内在の格子(Normative)が無警告で単一列へ縮退する(experiments/grid-sidebar-width §3 で実測) | 構文検査(rem の長さか)を行い、不正なら warn して既定 16rem へ退避。rem に限るのは仮置きだったが、単位 = rem が仕様に記録され解消した(2026-07。Grid 契約 alpha.1。container の rem 判断と threshold 裁定の適用)。数値集合も閉じた6段(8/12/16/20/24/32rem)で裁定し、契約 alpha.3 で enum 化した。仕様の門が会員性を検査する(GOVERNANCE の enum-default 規則)。実装は isWidthTier で退避する | A(解消済み。値語彙の裁定がオーナーから下り、enum で機械強制まで至った) |
| 21 | Sidebar | sideWidth / contentMin の不正値時の挙動。sideWidth の値語彙は Grid min と共有 | sideWidth: 閉じた6段(Grid min と同じ。契約 alpha.2)以外は warn して省略時(内容幅)へ退避。contentMin: 百分率(0〜100)以外は warn して既定 50% へ退避 | A(sideWidth の段は #20 と同時に解消。contentMin は B) |
| 22 | Sidebar | 契約は prop `side`(enum)と slot `side`(脇の中身)を同名で持つが、Svelte 5 は snippet も props も同一名前空間のため 1:1 で写像できない(Lit は attribute と slot が別名前空間で衝突しない) | 当初は slot 側を `sideSlot` へ写像した。裁定(2026-07)で仕様側が解消: GOVERNANCE §4 に「props / slots / events は名前を共有しない」が成文化・機械化され、Sidebar 契約 alpha.1 が slot を `aside` へ改名した。実装は追従して `aside` snippet を受ける | A(解消済み。実装の実測が仕様の統治規則を生んだ実例) |
| 23 | Sidebar | 折返しの実現機構(契約が expressive と明示) | flex-wrap + 本体 flex-grow 999 / min-inline-size(Every Layout 同型)。折れ点は算術どおりで viewport から独立(experiments/grid-sidebar-width §5 / §6 で実測) | B(Switcher HOLES #8 と同じ形) |
| 24 | Button | form 内での type の既定。実装が native の button を素のまま出すと HTML の既定により form 内で submit として振る舞う(独立レビューが実測で検出: form に入れて click すると submit イベントが発火する) | 仕様が裁定した(2026-07。Button.md §6)。Web 実装は type(button/submit/reset。既定 button)を持つ。中立契約には無い Web 層の取り決めのため META ではなく WEB 定数で持つ。既定 button で暗黙送信を塞ぎ、type=submit で送信されることを jsdom で実測した | A(解消済み。実測材料が仕様の裁定を生んだ) |
| 25 | Checkbox | リッチ label(リンク内包)の二重発火(リンク活性化が切替も発火しうる。契約 a11y の核心。field.md §6) | native の <label> がラップする形にし、HTML 仕様の「label の活性化は interactive content の子孫では何もしない」を土台にする。実装は独自の抑制を書かず native に委ねる。jsdom も実 Chromium も二重発火なしを実測(smoke は label テキストのクリックがトグルする対の検査で検出力を担保) | B(native 挙動で成立。実装 Done の条件を満たした) |
| 26 | Checkbox | 未 checked の hover の描き先。中立 border の hover 役は無い(TextField と同じ) | 契約 expressive のとおり、未 checked は面のウォッシュ(plain の soft-bg-hover/pressed)で描き、枠は中立のまま。checked は primary の bg-hover/pressed。描く先の無い状態宣言を避けた(state.md §4) | B(契約が描き先を指定済み) |
| 27 | Checkbox | indeterminate(第三の値)の Web 機構。属性でなくプロパティ | native input の .indeterminate を参照経由($effect + bind:this)で立てる。aria-checked=mixed は native がこの property から導く。切替時は onchange で indeterminate を下ろす(契約 events) | B |
| 28 | Textarea | extends の tokensRequired は union で継承されるため、子(Textarea)が親(TextField)の start/end を落としても、start/end 専用の spacing.inline.md を継承してしまう。svelte conformance は継承した全トークンの CSS 使用を要求する | Textarea の control は TextField の control 構造(flex + gap)を継承する形にし、同じ場所で spacing.inline.md を引く(子が1つで空ける相手は無く視覚効果なし)。発明ではなく構造の継承。仕様側の申し送り: extends が落とした slot 専用トークンを継承しない機構は無い(union のみ)。今は無害だが記録する | B・要検討(仕様の extends 機構。オーナー案件) |
| 29 | Switch | off トラックの塗り(plain の bg の濃さが妥当か)は seed(契約 Switch.md) | plain.bg を off、primary.bg を on に束縛。オーナー確認済み(2026-07): このまま進めてよい(実物を見て較正しうる) | B(契約が seed と明示。暫定で確定) |
| 30 | Icon | ツリーシェイク(name 文字列の実行時 lookup だと全 208 グリフが束に入る。実測)。中立契約は name(iconography.md §3)で変えられない | 二つの口(iconography.md §6 の Web 土地の声)。glyph 渡し(`import g from '@stemcell/icons/check'` の静的口)は Icon が全マップを import しないため使う分だけ束に入る。name は別モジュール resolve を動的 import して解決し(名を使うときだけの別チャンク)、glyph だけの消費者に全マップが混入しない。viewBox は定数直書き(@stemcell/icons から import すると同モジュールの全マップが引き込まれツリーシェイクが壊れる。実測で判明)。消費アプリで glyph 経由がエントリから未使用208を落とすことを実測。代償: (1)name は $effect の動的 import 解決なので SSR では出ない(glyph は $derived で同期に描き SSR で出る。独立レビュー B1 で $effect に描画対象を置く初版の欠陥を修正)。(2)name は初回だけでなくインスタンス毎に非同期段が入る(モジュールキャッシュ後も。テストが waitFor を要する)。viewBox は icons alpha.3 で map-free の ./meta へ分離し直書きの重複を解消(M2) | B(Web の土地の声。中立契約は name のまま。svelte/lit 両 Web 実装で共有すべき取り決め。glyph は Web 方言として Icon/meta.ts の WEB 定数・Icon.md に記録) |
| 31 | Radio/RadioGroup | 矢印キー・roving tabindex・disabled スキップ(web-keys arrows.radiogroup)の実現機構 | native の <input type=radio> を同一 name(RadioGroup が文脈で渡す)で束ね、ブラウザに矢印移動=選択・roving tabindex・disabled スキップを任せる(field.md の「native を使い再発明しない」)。実 Chromium で移動=選択・disabled スキップを実測。視覚は sibling の丸が描く(Checkbox と同型) | B(native 挙動で成立。実装 Done の条件を満たした) |
| 32 | Radio/RadioGroup | Radio は checked/change/invalid を持たない(値・change はグループ所有、error はグループ帰属。field.rules.json groupItems)。項目とグループを Svelte context で繋ぐ | RadioGroup が context(name・value・disabled・invalid・select)を getter 付きで渡し反応的に読ませる。checked は value 一致から導く $derived。change はグループが select 経由で発火。danger は契約どおりグループ所有: RadioGroup が CSS custom property で項目へ継承させ、Radio は継承 var を引く(Radio.css は danger を直接引かない)。これで RadioGroup 契約の danger トークンが RadioGroup.css で引かれ conformance が通る | B(グループ解剖の実装保証) |
| 33 | RadioGroup | fieldset の暗黙 role が group で aria-invalid/required を持てない(svelte-check 警告) | role="radiogroup" を明示(契約 a11y の role)。aria-labelledby で legend を指す。native の矢印キーは name 駆動で role に依らないため無影響 | B |
| 34 | Radio | 選択済みかつグループ invalid のとき、:checked ルールが --_border を circle に直接再宣言し、祖先 .sc-radio に効く invalid override を継承経由で潰す(選択済み項目の枠が danger に負ける。独立レビュー blocker が実測。契約が想定する「不正な組み合わせ」で発生) | data-invalid かつ :checked の circle に枠だけ danger を再宣言する(塗りは primary のまま)。playground の「選択済み × invalid」デモで実測、smoke が枠色=danger を検査(RED も実測) | A(解消済み。独立レビューが検出) |
| 35 | RadioGroup | required の native constraint validation。aria-required は立てるが個々の input に required 属性を付けない(native <fieldset required> は無意味) | 付けない。バリデーションのタイミングはアプリが持つ(field.md §3)。native form 送信に頼る消費者は自前でハンドリングする(独立レビュー minor。field.md §3 の方針と整合) | B |
| 36 | (仕様側) | `--motion-scale` の機構が実装に配線されていなかった。motion.md §6 は「環境設定を `--motion-scale` で受け、全 duration に乗算する。コンポーネントは分岐しない」と定め、Web 実装として `@media (prefers-reduced-motion: reduce) { :root { --motion-scale: 0 } }` まで指定している。しかし tokens が `--motion-scale: 1` を配信しているだけで、どの部品も乗算しておらず、Dialog / Popover / Toast / Tooltip / Drawer は各自 `@media` で 0ms を書いていた。結果は同じだが機構が仕様と違い、単一の源が無い状態だった | 仕様の機構へ揃えて解消。StemcellProvider に `--motion-scale: 0` を配線し、動きを持つ部品は `calc(var(--motion-X-duration) * var(--motion-scale))` で受ける。5部品の `@media` 分岐を撤去した。Toast の JS(退去後にキューから外す時間)も `prefers-reduced-motion` を読み直すのをやめ、同じ計算で出す。loop を使う3部品(Skeleton / LinearLoader / CircularLoader)だけは特例で `@media` を残す: 0 を掛けると動きが消え、loop の特例(停止/沈静)と衝突するため | A(解消済み。分岐が残るのは loop の特例と、scale を定義する Provider だけ) |
| 37 | CircularProgress / CircularLoader | `tokensRequired` の `shape.semantic.pill` を参照する場所が無い。円を線で描くため半径が無く、丸端は `stroke-linecap: round`、丸い輪は円の幾何が担う。適合検査は `var(--token)` の実在で照合するので、飾りの宣言を足さないと通せない | 仕様へ還元して解消。円形2契約から `shape.semantic.pill` を外した(prompts #58。CircularProgress alpha.2 / CircularLoader alpha.1)。線形2本は溝と帯の角として現に参照するので据え置き | A(解消済み。`tokensRequired` は「参照が必須のトークン」という HOLES #18 の決着を円形へ適用した) |
| 38 | LinearProgress / CircularProgress | 数字と帯の間隔(`spacing.inline.md`)が契約の `tokensRequired` に無い。`showValue` の置き方は Expressive だが、どこに置くにせよ間隔は要る | 実装は `spacing.inline.md` を引いている。契約へ足すのが筋(HOLES #18 と同型の欠落)。文字色は宣言せず周囲を継承する形にして、色トークンの欠落側は解消した | A・既知(仕様へ還元する。未起票) |
| 39 | Slider | `changeEnd`(操作の一続きの終わり)がキー操作でどう出るか。契約は「ドラッグ・キー操作の列が終わったこと」と書くが、native の `change` は矢印キー1打ごとに発火し、列を束ねない | native の意味論をそのまま採る(1打ごとに発火)。束ねるには独自のタイマーを発明することになり、何ミリ秒が正しいかに根拠が無い(第3条)。契約の「一続き」はドラッグの解放を主眼にした表現で、そこは native と一致する。SwiftUI の `onEditingChanged` / Compose の `onValueChangeFinished` もキーボードの列を束ねる観念を持たない | B(仕様は沈黙のままでよい。逐次が要る用途は `change` が担う) |
| 40 | Slider | 契約の `tokensRequired` に `color.app.fg-muted` が無い。description の文字を副次色へ落とすのに要る(TextField は同じ用途で契約に持つ)。あわせてトラックの太さとサムの径も SSOT に無く、実装が数値を直書きしていた | 仕様へ還元して解消。`color.app.fg-muted` を契約へ追加し、幾何は `slider.track-thickness` / `slider.thumb-size` として新設した(prompts #60・tokens #11。size.md §2 の例外列挙に Slider のサムを追加)。進行表示のトークンは流用しない(Material 3 が 4dp と 16dp で違え、本 DS も溝の中立段を別に引いているため)。実装は参照へ差し替え済み | A(解消済み) |
| 41 | Slider | サムの当たり判定。契約は size.md §4 の門を要求するが、`::-webkit-slider-thumb` の寸法は見た目であって当たり判定ではない。入力自体の高さ(1.5rem)で縦の当たりを確保し、横はトラック全域が受ける形にした | 入力の `block-size` を 1.5rem に取り、`touch-action: pan-y` で縦スクロールとの競合を避ける。実際の当たり判定と、タッチのドラッグがページスクロールに奪われないことは実機での確認項目(契約 a11y が検証項目として挙げている) | C(機械では測れない。実機の確認に依存) |
| 42 | Cover | 「器の高さいっぱい」の単位(契約が Expressive として開けている)。100vh は携帯で動くブラウザ UI の裏に下端が隠れる | `min-block-size: 100vh` を書いてから `100dvh` で上書きする。非対応環境は後段の宣言を無視して vh に残る(第7条の退避)。Dialog の下端で踏んだ揺れと同根の問題を先に塞いだ。実機(iOS / Chrome)で下端が UI に隠れないことを確認済み(2026-07-26) | B(仕様は沈黙のままでよい。実機で確認済み) |
| 43 | Frame | ratio の不正値時の挙動。比でない文字列は `aspect-ratio` の宣言ごと無効にし、比の窓(Normative)が無警告で消える | 構文検査(「横/縦」の正の整数比か)を行い、外れたら warn して既定 16/9 へ退避する。数値集合は裁かない(比の既定集合を凍結するかは Frame.md §1 の TODO) | B(HOLES #10 / #20 と同型の予防) |
| 44 | Center / Container | 二つの部品の CSS がほぼ同じ(論理方向の自動余白 + 段ごとの max-inline-size)。共有ヘルパへ寄せたくなる | 寄せない。適合検査は各部品の `<Name>.css` に必須トークンの `var()` が現れるかで照合するので(conformance/generate.ts)、共通ファイルへ移すと門が空振りする。重複は5行で、違いは段の集合と既定値 | C(検査の形が実装の形を縛る例。#7 / #19 と同根) |
| 45 | Reel | 焦点を受ける機構。契約は「スクロール領域はキーボードで操作可能でなければならない」を Normative に持つが、Web での実現(tabindex)は書いていない | 実際にあふれているときだけ `tabindex="0"` を立てる。器と帯の2枚構成にし、両方を ResizeObserver で見て測る(帯は中身に合わせて伸びるので、子の増減も帯の寸法変化として届く)。常時立てないのは、収まっている帯が中身の無い停留所になるため。この条件つきの形は Chrome と Firefox が focusable scrollers として実装した挙動そのもので、未対応の環境を自前で補う位置づけになる(第2条 / RFC 0010)。キーボード環境で矢印キーによる横スクロールを確認し、実機(iOS / Chrome)で横スクロールがブラウザの戻る操作へ連鎖しない(`overscroll-behavior-inline: contain`)ことも確認済み(2026-07-26) | B(仕様は沈黙のままでよい。実機で確認済み) |
| 46 | Reel | 焦点を受ける器に名前も role も無い(契約が role を持たず、label の prop も無い) | そのまま。native の focusable scroller も名前を持たない。役割を主張しないという共通則(layout.md §6)とも整合する。読み上げ環境での実際の聞こえ方は実機で確認する | C(機械では測れない) |
| 47 | Imposter | 器の外箱を行内に置くか塊として置くか。native に inline / block の対が無いため中立契約に持てない | Web 方言の `inline`(既定 false)として META ではなく WEB 定数で持つ(Button の type と同じ形。HOLES #24)。仕様側は Imposter.md §3 に Web 固有として分離して記録済み。行内の下地に重ねるとき、塊のままでは器が行幅まで広がって隅の重ねが行の端に着く | A(解消済み。仕様へ還元して Web 固有と明示した) |
| 48 | Imposter | 中央寄せの機構。transform で 50% 戻す定型は物理軸(x / y)しか持たず、論理の inset と縦書きで食い違う | 両端の inset を 0 にして自動余白で寄せ、箱が伸びないよう `inline-size` / `block-size` を `fit-content` にする。全て論理プロパティで書けるので RTL と縦書きで破綻しない | B |
| 49 | ToolCall | 段階の遷移をどう告知するか。契約は割り込み度(error は即時 / result は穏当)を定めるが、Web の機構と「何を読み上げるか」は書いていない | 結果と失敗それぞれの領域を最初から DOM に置き(空)、遷移で中身が入る形にした。`role="status"` / `role="alert"` を器に付け、読み上げられるのはアプリが差した結果や失敗の文そのものになる。段階名の文言(「完了しました」等)を DS が持つ形は採らない: 文言は DS 所有文字列で i18n の集約トリガーに触れ(i18n §4)、その機構が無い今それを先に作ることになる。領域を後から丸ごと挿入する形も採らない(支援技術によって拾われないことがあり、一度だけ確実に届くを満たせない) | B(仕様は沈黙のままでよい。読み上げの実地確認は残る) |
| 50 | ToolCall | status が result / error なのに対応するスロットが無いとき。契約はどちらも optional で、無いときの扱いを書いていない | warn する。完了や失敗が枠の色だけの出来事になり、視覚だけに頼らない線(WCAG 1.4.1 の同型)に触れるため。描画は止めない(アプリの正当な状態でありうる) | B |
| 51 | ToolCall | busy の図形をどう描くか。進行表現は Expressive で、契約は寸法のトークンを挙げていない | 初版は輪を自前で描いた(寸法は文字に従わせ 1em)。オーナーの指示で円形の不確定ローダーの合成へ変えた。待ちの絵を二度描かないのが決め手で、loop の時間と加減速も reduced-motion での沈静もあちらが持つ。これに伴い契約から motion.loop を外した(prompts #63。参照する場所が器に無くなるため) | A(解消済み。仕様へ還元した) |
| 52 | Sources | 出典の列をどう受け取るか。契約は「項目ごとのスロット・スニペット・配列のどれかは各実装の表現」とだけ書く | 配列 + 項目のスニペットに写す(`items` と、項目を引数に取る `children`)。項目の器(li)は Sources が出す。`items` は契約の props ではなく既定スロットの Svelte での表現なので META.props に入れない(Button の type と同じ扱い) | B(契約が明示的に開けている) |
| 53 | Sources | 相互参照キーを Web でどう帯びさせるか。契約は「機構は表現」とし、aria-details を単独の手段にしないとだけ書く | 各項目の DOM の id に出典の id をそのまま載せる。断片リンク(#id)という最も確実に届く到達手段が開き、本文側の端(未起草)がそれを指せる。id が無い出典は warn する(UI は採番しない。source §3) | B(本文側の端が起草されたら両側から確認する) |
| 54 | (harness) | 契約が props を持たない部品(Sources)で、生成される適合検査が型エラーになる(空オブジェクトの Object.entries が unknown) | 生成器のテンプレートに型注釈を付けた。props を持たない契約が初めて現れたことで露出した穴 | C(生成器の穴。解消済み) |
| 55 | ToolCall | 円形の不確定ローダーは「何を待っているか」の名前(label)を必須で要求するが、ToolCall はその図形を支援技術から隠す(実行中であることは領域の aria-busy が伝える。契約 a11y) | 包んで隠し、label は空で渡す。隠す以上その名前は誰にも届かないので、届かない文字列を DS が発明しないほうを採った(段階名の文言を持たない判断と同じ筋。HOLES #49)。装飾として使うことを認める口をローダー側に開ける形もありうるが、消費者が現れて必要が立証されてからでよい | B(合成の摩擦。仕様は沈黙のままでよい) |
| 56 | Reasoning | 生成中の視覚。契約は明滅やカーソルを Expressive として開けているが、必須トークンに motion を持たない | 初版は足さなかった(意味は名前が運ぶため)。オーナーの指示で、名前の上を光の帯が走る表現を足した(読み込みの代役と同じ手触り)。光は薄い側から濃い側へ振れる: 白や地の色を重ねる形は、Skeleton が独立レビューで差し戻された経緯のとおり明るいテーマで見えず、文字の上ではコントラストも削る。帯のところだけ濃くなる形なら、いちばん薄いところでも副次色の可読性を割らない。時間と加減速は `motion.loop` を引くが、契約の `tokensRequired` には挙げない: 進行表現は Expressive で、必須にすると飾りを持たない実装が門で落ちる(ToolCall から loop を外したのと同じ線)。`tokensRequired` は参照が必須のトークンの集合であって、表現の希望を書き留める欄ではない。reduced-motion では光を消して素の文字へ戻す(待ちの輪を低速へ沈静させたのとは判断が違う。あちらは図形が唯一の手がかりで、こちらは名前の文字が意味を語っている) | B |
| 57 | Reasoning | 完了が支援技術へ届く経路。契約は「名前が段階を語る」ことを期待するが、それはアプリの作法である | トリガーの名前を穏当な live region(role=status)に置き、名前が変わったことを一度だけ告げる。名前が段階を語らないアプリでは完了が届かないが、機械では捕まえられない(Badge の dot × label と同型)。DS が段階の文言を持つ道は採らない(i18n の集約の判断に荷物が増える。HOLES #49 と同じ理由) | C(仕様が限界を明記済み。実地の確認が残る) |
| 58 | Reasoning | 名前が別の文言へ移るときの動き(ChatGPT のように段階ごとに文言が差し替わる)。名前はアプリが差すスニペットなので、いつ変わったかを prop では知れない | 描かれた文字の変化を MutationObserver で拾い、古い文字の面を live region の外(兄弟)に一時的に置いて、文字が貼り付いた立方体を上へ一面ぶん回す(オーナー指示 2026-07-26)。立方体は DOM で組み立てない: 生きている文字を別の器へ入れ直すことになり、Svelte が持つ節点を動かしてしまう。面ごとの変換を composed で書き、立方体の中心を文字の面より半分ぶん奥に置いて、回り終わりが無変換に戻るようにした(fill が残っても文字が手前に寄って大きく見えない)。影を live region の中に入れないのは、古い文言が支援技術へもう一度届くため。合図に prop を足す道(変化のキーを受ける)は採らない: 動き自体が Expressive なのに、そのために中立でない prop が公開表面へ出る | B(実際の動きは実ブラウザでの確認が残る) |
| 59 | (共有) | JS で動きを作るときの reduced-motion の受け方。CSS は `calc(duration * --motion-scale)` で受けるが、JS には器が無かった | `internal/motion.ts` の `resolveMotion` に集約した。計算済みスタイルから意味層の duration と `--motion-scale` を読んで掛ける。部品は `prefers-reduced-motion` で分岐しない(motion.md §6)。値が読めない環境では 0 に落ち、動かないだけで壊れない | B |
| 60 | Sources | キー付きの反復に生の id を使うと、結べない項目が並んだとき衝突して一覧ごと落ちる。二通りある: 逐次配信の途中でまだ id が付いていない出典が2件以上並ぶ場合と、同じ出典を何度も引く回答で同じ id が繰り返される場合(事実標準では珍しくない。Vercel は引用ごとに source part を出し、Anthropic の citation は位置で表され安定 id を持たない) | 結べない項目(id 無し / 同じ id の2件目以降)は位置でキーを作る。DOM の id は最初の1件にだけ置く: 同じ id が2つあると断片リンクがどちらを指すか決まらず、HTML としても不正になる。採番はしない(source §3)ので、結べないことは警告で知らせる | B |
| 61 | Reasoning | 名前が速く移り変わる配信では、去る文字の影が積み上がりうる(トークンごとに文言を書き換えるアプリ等) | 前の動きが途中なら畳んでから次を始める(影は同時に一つ)。なお名前が毎トークン変わること自体は契約が期待していない形で(段階を語る名前が変わる)、支援技術への洪水にもなるが、それは実装では捕まえられない | B |
| 62 | (トークン側) | `color.app.fg-subtle`(補助の文字)が明暗テーマで同じ値(#5c6c80)であり、暗い地(#292f36)の上では 2.6:1 しかない。本文の床(4.5:1)を割る | Reasoning の光の帯で、振れ幅を広げようとして触れて分かった。使わず `fg-muted`(暗いテーマで 5.2:1)を土台にした。トークン側で暗いテーマの値を起こす必要がある(明るいテーマの値をそのまま流用している疑い) | A・既知(トークンへ還元する。未起票) |
| 63 | Tabs | パネルが焦点を受けるか。契約は帯とタブの焦点しか書いていない | 常に `tabindex="0"` を置く。帯から中身の領域へ辿り着く経路がここしか無く、APG Tabs Pattern と Radix も同じ形である。Reel で「中身の無い停留所を作らない」と判断したのとは事情が違う: パネルは必ず中身を持つ(スロットが必須) | B |
| 64 | Tabs | 矢印の論理方向。web-keys は「RTL では Left / Right の意味が反転する」と共通則で書くが、機構は書いていない | 押された時点で帯の計算済みスタイルから `direction` を読む。属性(dir)を辿ると祖先で指定された場合に拾えず、CSS で切り替える形も拾えない | B |
| 65 | Tabs | 矢印で選択を動かした後、焦点をどう追わせるか。値の更新はアプリなので、押した直後の DOM はまだ古い | `tick()` の後に `aria-selected="true"` のタブを探して焦点を移す。アプリが選択を拒めば value が変わらず、焦点も動かない(拒否がそのまま伝わる) | B |
| 66 | NavList | 契約の expressive notes は「行き先は Link の合成」と書くが、実装は native の `<a>` を直に描いた | 脇の一覧の項目は行の見た目で、文中の文字リンクとは別物である。Link を並べると文字リンクの色と下線が付き、行の器と衝突する。到達性は native の `<a href>` が無償で満たすので、合成の必要が無い。さらに Link は `aria-current` を受ける口を持たず、現在地の表明ができない(rest props を開けていないため)。仕様側の注記を実装に合わせて還元する | A・既知(仕様へ還元する。未起票) |
| 67 | Pagination | 位置の表示に現在地をどう持たせるか | 中央を選択(Select の合成)にしたので、選ばれている値がそのまま現在地を語る(オーナー指示 2026-07-26。契約 alpha.1)。初版は静的な文字に `aria-current="page"` を置いていたが、飛ぶ手段が無かった。番号を列に並べる形は採らない(頁が増えるほど省略の規則が要り、狭い画面で折り返して崩れる) | A(解消済み。仕様へ還元して契約を改めた) |
| 68 | (横断) | 当たり判定の床に独自の数値(2.75rem)を書いていた。密度で詰めてよい層(見た目)と、詰めてはならない層(当たり判定)を混ぜていた | 他の部品と同じ 24px(1.5rem)へ揃えた。内側の余白は spacing の段を引いているので、compact では自然に詰まり、床だけが残る(size.rules.json: 段と密度は見た目を詰めてよく、割ってはならないのは当たり判定だけ)。タブの選択の下線も独自の 2px をやめ、境界の原始の2倍で表した | B(気づきはオーナーの指摘。密度対応の確認から) |
| 69 | Checkbox / Radio | 選択の箱の寸法が実装の直書き(20px)だった。SSOT に無いので、他の実装と揃う保証が無い | トークンへ移した(`selection.size` 16px / `selection.mark-size` 8px。tokens alpha.8、size.md §2 の例外列挙)。チェックの印も箱に比例させ、印の線の太さは境界の原始の2倍で表した。箱は `box-sizing: border-box` にして、トークンが外形の一辺を指すようにした(含めないと枠のぶん実寸が大きくなり、同じ値を指しているのに実装ごとに割れる)。16px は業界の常態(shadcn/ui・Carbon・Ant Design・Fluent が同値、Material 3 は 18dp) | A(解消済み。Slider の幾何と同じ形の還元) |
| 72 | (作業環境) | トークンの版を上げた後、走らせたままの dev サーバが古いトークンを配り続け、新しい CSS(`var(--selection-size)`)だけが届いた。参照先が無い宣言は無効になり、選択の丸が幅ゼロに潰れて点だけが見えた | サーバを入れ直し、`node_modules/.vite` を消す。実装の欠陥ではないが、見た目は「部品が壊れた」ようにしか見えないので記録する。トークンの版を上げたら dev サーバを入れ直す | B(作業手順。実装は正しかった) |
| 70 | Popover / Tooltip | 浮かぶ面が影だけで地から浮いていた。強制配色(forced-colors)では影が落とされるので、面と地が同じ色で溶ける | 髪の毛ほどの縁(`color.app.divider`)を引く。相互作用する部品の境界ではないので 3:1(WCAG 2.2 SC 1.4.11)の対象ではない。業界も同じ形(shadcn/ui と Radix、Carbon、macOS のメニュー) | A(解消済み。elevation.md §3 へ還元) |
| 71 | (横断) | 中立の輪郭が二つあった。plain な outlined の Button は `semantic.plain.border`(light で 6.45:1)、文字の欄は `app.border`(3.17:1)を引いており、ページ送りの中央と左右で色が割れていた | トークン側で light の `semantic.plain.border` を `app.border` へ揃えた(dark は元から同値だった)。あわせて WCAG 1.4.11 の射程を color.md §6 に表として置いた: 文字や絵が存在を語る部品(Button / Tag)は 3:1 の対象外、空の欄と未選択の箱は対象 | A(解消済み。実装ではなくトークンの較正の問題だった) |
| 73 | NavList | 行の間隔。意味層の縦の段は sm(8px)が最小で、脇の一覧の行の律動にはそれでも広い(離れて浮かぶ札の列に見える) | 間を空けない(gap: 0)。行は続けて置き、行の高さは内側の余白(spacing の段)が作るので、密度を compact にすれば行そのものが詰まる。4px を指したいなら意味層に xs の段が要り、それは spacing.md の改正(RFC)になる。小域の原始を直接引く道は採らない(spacing.md §6 が意味層を使えと定める) | B(仕様側の余地として記録。段を増やすかは必要が立証されてから) |
| 74 | Accordion | 契約 alpha.0 が器に `role: "button"` と `focusRing: true` を書いていたが、器はトリガーを自分では描かない(各項目は Disclosure の合成)。適合検査が「必須の焦点の輪のトークンが CSS に無い」と落とした | 仕様へ還元して解消(契約 alpha.1)。器は role を持たず焦点も受けない。button の意味論も輪も項目の契約(Disclosure)が持つ。APG Accordion Pattern も器に役割を要求しない。合成した部品が持つものを器が宣言しないという線(ToolCall から motion.loop を外したのと同じ形) | A(解消済み。門が設計の誤りを捕まえた) |
| 75 | Disclosure | Accordion の項目に「今は開けない節」が要るが、Disclosure に口が無かった | 仕様へ還元して `disabled` を開けた(契約 alpha.1)。抑えるのは操作であって値ではない(既に開いている中身は閉じない)。トリガーは焦点を受けたままにする(開けない節があること自体が読める情報。state.md §5)。native の `<summary>` に disabled 属性は無いので Web の表現は aria-disabled | A(解消済み。Radix の Collapsible も単体で持つ観念) |
| 76 | List | 区切り(divided)と縦の律動をどう両立させるか。間を空けたまま線を引くと、線が前の項目に貼り付いて見え、どちらの区切りか読めない | 区切りを引くときは間隔を項目の内側へ移す(gap を 0 にして padding-block を持たせる)。線が項目と項目の「あいだ」に立つ | B |
| 77 | (共有) | 暦日の計算をどこに置くか。日付は4部品が共有し、地方時で計算すると夏時間の切替日に1日ずれる | `internal/date.ts` に集約し、計算はすべて UTC で行う(値は暦日であって時刻ではない。date.md §2)。地域の慣習は環境から借りる: 桁の並びは `Intl.DateTimeFormat.formatToParts`、桁の名前は `Intl.DisplayNames`(type: dateTimeField)、週の始まりは `Intl.Locale.getWeekInfo()`、月名と曜日名は `Intl.DateTimeFormat`。DS はこれらの文字列を1つも持たずに済んだ | B |
| 78 | DatePicker / DateRangePicker | 暦を開くトリガーの ARIA。Popover はアンカーの ARIA を張らず、IconButton は `aria-haspopup` / `aria-expanded` を受ける口を持たない | トリガーを器が自分で持つ(Menu と Select が同じ形。「開く部品が自分のトリガーを所有する」)。自分で持つ以上、焦点の輪と当たり判定もここが担う。契約は `focusRing: false` / role 無しで書いていたので、仕様へ還元する | A・既知(仕様へ還元する) |
| 79 | DatePicker / DateRangePicker | 契約は暦を開く操作の名前を slot で持つが、絵だけの操作の名前は文字列で足りる(Icon の label・CircularLoader の label と同じ形)。slot にすると合成した部品へ渡せない | 文字列の prop で実装した。仕様へ還元する | A・既知(仕様へ還元する) |
| 80 | DateField | 契約は min / max を「これより前は入力できない」と書くが、native の `<input type="date">` は範囲外も打てて :invalid になる(検証の時機はアプリが持つ。field.md §3) | 矢印での増減は範囲の外へ出ない(affordance が押し戻す)。打ち込みは通し、範囲の判定はアプリが持つ。仕様の言い回しを実装に合わせて還元する | A・既知(仕様へ還元する) |
| 81 | (アイコン) | アイコンの名前が `calender` と綴られている(正しくは calendar)。日付の部品が最初の消費者 | そのまま使った。名前の変更は配信物の破壊的変更なので、icons 側の版で扱う。ここでは記録に留める | A・既知(icons へ還元する。未起票) |
| 82 | DateField | 欄が器いっぱいに広がる。field は fill 側という裁定(layout.md §2)どおりだが、日付の中身は常に10文字で、広げても情報が増えない。1400px の箱に「2026 / 09 / 29」が入り、桁が左端へ寄って押しにくくなる | 幅の上限を持たせる(オーナー裁定 2026-07-26)。fill のままで、幅の段の閉じた集合(8/12/16/20/24/32rem)から 12rem を上限に採る。本文が 66ch で頭打ちになるのと同じ形の例外で、layout.md §2 へ還元する | A・既知(仕様へ還元する) |
| 83 | DateRangePicker | 2つの欄が行を占めきり、暦を開く操作が次の行へ落ちていた | 欄を伸ばしきらない(`flex: 0 1 auto`)。上限を持つようになったので、2つの欄と暦が同じ行に収まる | B |
| 84 | DateRangePicker | 対が揃った時点で暦を閉じていた。期間は「選んで終わり」ではなく、両端を見比べながら詰める操作である(オーナー指示 2026-07-26) | 揃っても閉じない。揃った後にもう一度押したら、そこから新しい期間を取り直す(業界の常態)。閉じるのは明示の退出(Escape・外側の押下)で、それは合成した Popover が持つ。単一の日付(DatePicker)は選んだ時点で閉じる: あちらは1回で決まる | B(閉じる時機は Expressive。仕様の Expressive の欄へ書き足す) |
| 85 | Calendar | 狭い器で格子が枠からはみ出し、広い器では逆に横へ伸びきって日が遠くなった。最初は7列を固定幅で組み、次に器の幅を分け合わせて直そうとして、今度は間延びした | 格子は中身の幅で立つ(伸ばさない)。日は2桁までなので列の幅はそこで決まる。月は横に並べ、入らなければ折り返す。日付の欄に上限を持たせたのと同じ理由で、広げても中身が使えない(layout.md §2 の例外)。月の見出しは各格子の caption へ移した(折れたときに見出しと格子がずれない) | B |
| 86 | Popover / Tooltip | (廃止。#91 で組み直した) 画面の端に近いアンカーで、浮かぶ面が横に見切れていた。`position-try-fallbacks` が `flip-block` だけで、inline 方向の衝突を見ていなかった。JS の退避経路も left を押し戻していなかった | 反転の候補に inline とその組を足し(`flip-block, flip-inline, flip-block flip-inline`)、幅を画面から溢れない値で頭打ちにした。JS の退避側も画面内へ押し戻す。Tooltip はアンカー中央に置くので、幅の半分を見込んで挟む | B(overlay.md §5 は反転を Expressive としており、仕様は沈黙のままでよい) |
| 87 | (横断) | 動きを `--motion-scale` で受けていない部品が10本残っていた(Button / Checkbox / IconButton / Menu / Radio / Select / Switch / TextField / Textarea / DateField)。HOLES #36 で機構を通したとき、overlay の5本しか直していなかった | 全部へ掛けた。reduced-motion で hover や切替の動きも止まる(それまでは overlay だけが止まり、欄と操作は動き続けていた)。loop を使う3本(Skeleton / 進行表示のローダー)は特例のまま(motion.md §6) | A(解消済み。#36 の取りこぼし) |
| 88 | 日付の部品 | hover などの手触りが無かった(桁・日のセル・暦を開く操作) | feedback の意味層で足した。桁は打てる場所なので指を置くと面が薄く点き、打っている桁は面で示す(焦点の輪だけではどこを編集しているか読みにくい)。日のセルと暦を開く操作にも hover と押下を入れた | B |
| 89 | DatePicker / DateRangePicker | 暦を開くと面が白いだけの箱になった。浮かぶ面の幅はアンカーに一致するのが Popover の既定で、ここのアンカーは絵だけのボタン(24px)である。暦は入らない | 消費者の口(`--sc-popover-inline-size` / `--sc-popover-min-inline-size`)で中身から決まる幅へ上書きする。あわせて暦の格子に幅の床を敷いた: 器の幅が中身から決まる文脈では `inline-size: 100%` が測れず、列が潰れる | B(Popover は既に口を開けていた。使う側の落ち度) |
| 90 | Popover / Tooltip | (廃止。#91 で組み直した) 縦に長い面(2ヶ月の暦)が画面の下で切れていた。最初は画面の高さで頭打ちにしたが、それでは直らない: 面は画面の下の方に置かれるので、測るべきは「アンカーから画面の端までの空き」であって画面の高さではない。しかも頭打ちで溢れなくなると、反転(position-try)がそもそも起きなくなる | 空きのセル(`position-area`)へ置く形に組み直した。セルが包含ブロックになるので、`100%` がそのまま「空き」を指し、幅も高さも空きで頭打ちにできる。入らないぶんは面の中で送る(中に焦点を受ける要素があるのでキーボードでも辿り着ける)。退避経路(anchor 非対応)では JS が空きを変数で渡す。Tooltip の中央寄せもセルが持つようになり、自前の translate が要らなくなった | B(位置決めは Expressive。仕様は沈黙のままでよい) |
| 91 | Popover / Tooltip | 面が画面から見切れる件は #86 と #90 の形では直らなかった。入るかどうかを面の大きさで測って向きを決めると、中身が伸び縮みするたびに向きが変わって結果が読めず、頭打ちで溢れを消した瞬間に判定そのものが働かなくなる(オーナー差し戻し 2026-07-26) | 三段に組み直した。(1) 上下はトリガーが描画領域のどちら側に居るかだけで決める(面の高さを見ない)。(2) 左右はトリガーの中心に揃え、その揃えでははみ出すときだけ位置で寄せる(揃えを変えても幅は変わらないので堂々巡りにならない)。(3) 残るはみ出しは横へ押し戻し、面が描画領域より大きいときだけ、その向きの空きで頭打ちにして中で送る。#86 と #90 の仮置きは破棄。overlay.md §5 へ還元した | A(解消済み。仕様へ還元。#86 / #90 を廃す) |
| 92 | Popover / Tooltip | 中央揃えに transform を使い、出現アニメの keyframe も transform を持っていた。アニメの間だけ揃えが消え、面が右から中央へ滑って着地した(実測 left=512 → 366、400ms) | 揃えと押し戻しは transform、出現の動きは translate と、別のプロパティへ分けた。同じ手段を取り合わせない。overlay.md §5 へ還元した | A(解消済み。仕様へ還元) |
| 93 | Button / IconButton / Tag | 強調度で高さが変わっていた。枠を持つのは outlined だけなので、同じ段でも filled / soft / text が 2px 低い。欄は枠を持つので、フォームの一行に欄と filled のボタンを並べるとボタンだけが低い | 枠を持たない強調度も同じ太さの透明な枠で場所を取る。高さを固定して揃える形は size.md §2 が禁じている(文字の折返しやフォント拡大で内容が箱を突き破る)。emphasis.md §3 へ還元した | A(解消済み。仕様へ還元) |
| 94 | Calendar | 期間の両端が真四角だった。期間の中のセルは帯を継ぐために角を落としており、その規則が両端にも掛かっていた。帯を端のセルの縁まで敷くと、丸めた角の外側へ回り込んで角が飛び出て見える | 面と帯を擬似要素へ分けた。面は shape.semantic.control の丸みと連続曲率を持ち(擬似要素は corner-shape を継承しないので個別に当てる)、帯は面の下へ敷いて端のセルの中央で止める(inset-inline-start / -end の 50%。RTL でも向きが入れ替わる)。Calendar.md へ還元した | A(解消済み。仕様へ還元) |
| 95 | DateRangePicker | 対が揃った後の押下をどう読むか。取り直す形から始め、外なら伸ばす案、直近に動かした端を動かす案と移った(オーナーの指示で三度変えた) | 押した日を必ず期間の下限にして選び直す(内も外も区別せず、端そのものを押したときだけ何も変えない)。端を動かす案は「直近に動かした端」という見えない状態を要求し、覚えていなければ押す前に結果が分からない。近いほうの端を動かす案は内側で意図と逆を向く。Stripe も下限で選び直す形(オーナーが実機で確認)。DateRangePicker.md へ還元した | A(解消済み。仕様へ還元) |
| 96 | DateRangePicker | 選び直しのたびに暦が始まりの月へ跳んでいた。start を読む effect が月を書き戻していた | 見えている月は開いた時点の始まりから始まり、以後は利用者のページ送りでしか動かない。期間は両端を見比べる操作なので、見ている場所を器が奪ってはいけない。DateRangePicker.md へ還元した | A(解消済み。仕様へ還元) |
| 97 | Calendar | 下限だけ置いた途中に、今押せばどこまでが期間になるかが読めなかった(仕様は予告の塗りを Expressive として置いていた) | 確定した対と同じ帯で予告する。相手は指を置いた日で、指が無ければ焦点の日(キーボードでも同じ予告が見える)。下限より前は予告しない(押しても期間にならず下限が置き直されるだけ)。予告は選択ではないので aria-selected は対だけに従う。帯の属性を data-in-range から data-band へ改めた。Calendar.md と date.md §9 の宿題(格子1本で表せるか・範囲の格子を別部品にするか)を閉じた | A(解消済み。仕様へ還元) |
| 98 | (横断) | 文字の描き方(Apple 系の平滑化と和文の字送り)がどこにも無かった | 地の面へ三宣言を置いた(-webkit-font-smoothing: antialiased / -moz-osx-font-smoothing: grayscale / font-feature-settings: 'palt')。太さを明暗テーマで揃えることと、和文の穴を詰めること。Web / Apple 系だけの機構なので、その旨を明示して typography.md §5 へ還元した | A(解消済み。仕様へ還元。オーナー提供 2026-07-26) |
| 99 | Calendar | iOS(WebKit)で選ばれた日の面が出ず、白い文字だけが残って読めなかった。面と帯を擬似要素の絶対配置＋負の重ね順で描いていた | 表のセルに対する絶対配置は WebKit でセルではなく表を基準にすることがあり、負の重ね順と組むと面が地の下へ回る。背景と通常フローだけで組み直した(帯はセルの背景、面はセルの中の箱)。見え方は変わらない。仕様側は Calendar.md の帯の規則が既にあり、描き方は Web の表現なので沈黙のままでよい | B(Web の描画順。仕様は沈黙) |
| 100 | Calendar | 組み直しの過程で列の幅を最小幅で書き、狭い器で格子が飛び出した(実機で発覚) | 表の指定幅は「好ましい幅」なので、最小幅ではなく指定幅で書く。器が狭ければ表が縮む(320px で列 36px、390px で 45px、広い画面では 52px)。「格子は中身で立ち狭ければ縮む」は仕様の言葉が無かったので Calendar.md へ還元した | A(解消済み。仕様へ還元) |
| 101 | DateField | 桁が span だったので触点でソフトキーボードが出ず、実機で値を変えられなかった | native の入力要素へ置き換え、inputmode=numeric で数字の鍵盤を頼む(意味は spinbutton のまま)。数字は keydown ではなく入力イベントで受ける(ソフトキーボードは打鍵を keydown に載せない環境がある)。DateField.md へ「桁は打てる要素で受ける」を還元(RFC 0017) | A(解消済み。仕様へ還元) |
| 102 | DateField | 触点で 16px 未満の欄に焦点が乗ると iOS がページごと拡大する。桁が入力要素になったことでこの挙動に掛かった。同じ狙いの規則は前からあったが、当てる先が存在しない要素(.sc-datefield-input)で効いていなかった | 桁へ向け直し、あわせて4部品に写し取られていた同じ規則を StemcellProvider の一値へ寄せた。拡大を禁じる手は採らない(利用者から拡大縮小を奪う。WCAG 2.2 SC 1.4.4)。field.md §2 へ還元 | A(解消済み。仕様へ還元＋実装の重複解消) |
| 103 | DatePicker / DateRangePicker | 狭い画面では暦がアンカー従属のままだと入りきらない。位置決めを直しても、面が画面より広ければどちらへ開いても切れる | 器の広さで overlay の類を変える(狭い画面は modal 類＝下端のシート)。RFC 0017 として overlay.md §5 へ足し、閾値は breakpoint.medium 未満。CSS のメディアクエリは custom property を読めないので、実装は算出値から閾値を取る(値は直書きしない) | A(解消済み。RFC＋仕様へ還元) |
| 104 | (作業環境) | dev サーバが古いブランチ/古い版を配り続け、実機の症状(面の見切れ)を実装の欠陥だと二度誤診した | ブランチを切り替えたら dev サーバを入れ直す。#72 と同じ罠で、今回は私が踏んだ。実機の報告を受けたら、まず配っている版を確かめる | B(作業手順) |
| 105 | Table | 仕様は「セルは1行に収め、収まらない分は切り詰める」と書くが、実装は切り詰めない。列に幅を与えていないので、長い中身は表そのものを広げ、器の中で横へ送る形になる(切り詰めが起きるのは消費者が列に幅を与えたときだけ) | 実装は折り返さない側だけを持つ。仕様の言い回しを「折り返さない。はみ出す分は表が広がり、器の中で送る」へ還元する | A・既知(仕様へ還元する) |
| 106 | Table | 貼り付いた列(sticky)の面が地の面で塗られ、選ばれている行や指を置いた行で、その列だけ色が変わらず行が分断されて見えた | 行が面の色を変数で持ち、貼り付いた列はその変数を引く。地の面は消費者が置いた場所(カードの上か地の上か)で変わるので、--sc-table-surface の逃げ道を一つ開けた(第4条) | B |
| 107 | Toast | 退去の待ち時間を JS で計算していた(--motion-exit-duration と --motion-scale を getComputedStyle で読み、掛けて setTimeout)。CSS と JS が同じ値を二重に持ち、単位の解釈も internal/motion.ts と別実装だった(いまトークンは ms なので実害は出ていないが、単位が変われば motion.ts 側だけ 1000 倍ずれる) | 退去の終わりは要素に走っているアニメーションが教える(Element.getAnimations() の finished)。時間の計算もキャッシュも消えた。アニメーションが無い場合(reduced-motion で 0、消費者が止めた)も finished は即座に解決するので、待ちっぱなしにならない | B(第2条の適用。仕様は沈黙のままでよい) |
| 108 | (横断) | 行の向きの判定 `getComputedStyle(el).direction === 'rtl'` が Tabs / Calendar / DateField に逐語で写っていた | internal/direction.ts の arrowKeys() へ集約し、判定を :dir(rtl) へ寄せた。算出値を読む形はスタイル再計算を起こすうえ、dir="auto"(中身から向きが決まる)を拾えない | B |
| 109 | (横断) | 間隔 prop の解決(段か大域の原始か・語彙外なら警告)が8つのプリミティブへ写し取られていた | internal/spacing.svelte.ts の useGap() へ集約(挙動は同じ)。Box だけは二値の inset を持つ別の形なので、今回は寄せていない | B |
| 110 | DatePicker / DateRangePicker | 狭い画面で面の類を変える処理(matchMedia の購読と Popover / Drawer の出し分け)が両部品に重複していた | internal/AnchoredPanel.svelte へ切り出した。RFC 0017 の規則(器の広さで類が変わる)を持つ場所が一箇所になる | B |
| 111 | Calendar / (トークン) | 選べない日の文字が disabled の色(明 2.47:1 / 暗 1.93:1)で、本文の床(4.5:1)を大きく割っていた。焦点に載る日をその色で描くと、読み上げられるのに読めない | 本文と同じ色で描き、選べないことは打ち消しで示す(色だけに頼らない)。薄い側(app.fg-muted)も採れない: 暗いテーマで浮かぶ面(#38414b)の上に置くと 4.2:1 で床を割る。地の面の上(5.48:1)なら足りるので、暗いテーマの fg-muted が浮かぶ面を勘定に入れていない。#62 と同根で、トークン側の較正が要る | A・既知(実装は解消。トークンへ還元する) |
| 112 | DateRangePicker | pattern が「候補を暦の面の中に置く」形を認めていたのに、器が面の中へ差し込む口を持っていなかった(面の中身は器が閉じて持っていた) | panelLead を開けた(契約 alpha.4)。候補そのものは持たない(何を候補に出すかはアプリの政策)。並べ方は器が持ち、広い画面では暦の横、狭い画面では暦の上へ回る。pattern の組み方(消費者が Cluster / Reel を選ぶ)も実物に合わせて直した | A(解消済み。仕様へ還元) |
| 113 | (pattern の確認) | 期間の候補を Select へ畳んで暦の面の中に置くと、面の中で面が開く。親の面が閉じないか、Escape がどちらを閉じるかが未確認だった | 実物で確かめた。親の面は開いたままで、Escape は内側から順に効く(native の popover の重なりがそのまま働く)。選択肢の面は器の外へはみ出して重なるが画面には収まる。触点では選択が環境のピッカーへ落ちるので、そこは実機の宿題(pattern §3 へ還元) | B(実装の変更なし。pattern の裏取り) |
| 114 | CodeBlock | 行番号を折り返したとき、続きの行をどこから始めるか。仕様は「折り返した先には番号を振らない」とだけ定める | 続きの行も本文の始まりに揃える(番号の下へ潜らせない)。当初は器の端から始めており、実機で潜って見えたので直した。行を block にすれば字下げできるが、そうすると改行を文字として持てず複写がブラウザ任せになるので採らない。番号(生成内容)を本文の幅の外へ負の margin で引き出し、その幅ぶん pre の padding を内側へ寄せる形にした。行は inline のままなので改行は文字として残り、複写は元のとおりになる | B(解消済み。見た目と複写の両立。続きの行を本文の始まりへ揃えることは CodeBlock.md へ還流済み)
| 115 | CodeBlock / (レイアウト原始) | 横へ送れる器を Stack や Cluster の中へ置くと、親が中身の最大幅まで広がって器が縮まず、ページごと横に伸びる。器は自分に min-inline-size: 0 を持つが、親の連鎖までは面倒を見られない | 実物(playground)では送れる器を段の直下へ置いて回避した。Table も同じ性質を持つ(あちらも自分にだけ 0 を持つ) | A(解消済み。layout.md §2 に「横へ送る器を包む器も縮む」を足した。2026-07-27) |
| 116 | CodeBlock | 行番号を支援技術から外す機構。仕様は「隠す」とだけ定める | CSS の生成内容に代替文字列を与える(`content: counter(sc-line) / ''`)。実要素にすると複写に混ざり、生成内容のままだと読み上げに載る。両方を満たすのはこの形だけだった。Chromium で、番号の有無で a11y の名前の一覧が変わらないことを実測した | B(機構は Web の表現。native は各々の機構を持つ) |
| 117 | Stat / (トークン) | 暗いテーマの副次の文字が面の上で床を割っていた。fg-subtle は light の値のままで最も暗い面に対しても 2.52:1、fg-muted はカードや表が使う面で 4.20:1。border は面ごとに較正されていたのに、文字は一度も面ごとに問われていなかった | tokens alpha.10 で fg-muted を gray.200、fg-subtle を gray.300 へ上げ、check:palette に文字の検査を足した(foreground と link は四面すべて、副次の二役は実際に載る二面で強制)。modal 面の残りは印字だけにした(そこで床を満たす段は 50/100 しか無く、強制すると三役が同じ色になる) | A(解消済み。残りは color.md §11 のパレット全体の author へ) |
| 118 | Stat | 変化の向きだけを渡したとき、支援技術に何も届かない(印は装飾、色は見えない)。契約は当初 support を任意にしていた | 契約 alpha.1 で「trend を渡すなら support も渡す」を明記し、実装は警告を出す(Switcher の不正値と同じ warn の形。器が持たない文言を器が作ることはできない) | A(解消済み。実装が仕様の穴を見つけた形) |
| 119 | NumberField | 増減の操作の置き場所は Expressive だが、実物で二つの制約が出た。縦に積むと当たり判定の門を割り(欄の高さは打つ文字が決めるので md では約 42px、二つで分け合うと 21px。実測)、欄をまたいで左右に離すと続けて押すのに指と視線が往復する(利用者の指摘) | 二つを隣り合わせて欄の後ろへ横に並べた(欄 → 減らす → 増やす)。行高いっぱいに伸ばして 43x30px になり門(24px)を満たす。増減が居る側だけ器の横 inset を手放し、値の側は残す(手放すと打つ文字が枠に触れる)。二つの間は空けず線も引かない(二つで一組の操作なので、離すと別々に見える)。押下では焦点を奪わない(pointerdown を止める。焦点が欄から外れると続けて打てない) | B(置き場所は Expressive のまま。縦積みの制約と隣り合わせる理由は契約の expressive へ還流済み。2026-07-27) |
| 120 | Rating | 読み取りの塗り分けを「列全体に対する割合」で切ると、段の間隔(gap)のぶん割合と段の位置がずれる。4.2 の 0.2 が隙間の上に落ち、五つ目が塗られない | 間隔を星の箱の中(padding)へ移し、列の隙間を 0 にした。段ごとに同じ幅になるので、割合がそのまま段の位置になる(実測: 塗り 84%、五つ目の 20% まで塗られる) | B(Web の表現。native は各々の塗り分けの機構を持つ) |
| 121 | DateField / TimeField | 打ちかけの数字を溜める buffer が、桁を移らずに隣の桁へ焦点だけ動かしたときに残る。月に 1 を打ってから日を押して 2 を打つと、日が 12 になる(TimeField を作って初めて踏んだ。DateField にも同じ穴があった) | 溜めた数字と一緒に「どの桁で打ったか」を覚え、別の桁の入力が来たら捨てる。両方の部品を直し、回帰の試験を足した | A(解消済み。二つ目の実装が一つ目の穴を見つけた形) |
| 122 | (横断) | 桁を持つ欄の手触り(端で回る増減・右から溜まる打ち込み・埋まったら次へ送る)が DateField に閉じており、TimeField が同じものを必要とした | internal/segments.ts へ純粋な計算として抜き、両方が引く。焦点の移動と値の公開は、値の意味を知っている各部品に残した(日付は実在する日か、時刻は HH:mm を知っている) | B(重複の解消。挙動は変えていない) |
| 123 | TimeField | 午前・午後を桁として置いたら触点で詰んだ。打てる欄なので iOS が仮名の鍵盤を出すが、そこに打てる文字が無い(a / p は英字の鍵盤にしか無い)。押して回す形へ直したが、今度は押せることにも変わったことにも気づけなかった(どちらも実機で判明) | 桁をやめて二択にした。午前と午後の両方を見せ、選ばれている側に面を持たせる(radio の群)。桁は数の桁だけになり、打てない桁という例外が消えた | A(解消済み。仕様の「午前・午後も桁である」を実機が否定した。契約 alpha.1 へ還流) |
| 124 | DateField / TimeField / DatePicker / DateRangePicker | 桁を持つ欄の背が、同じ段の文字の欄より低かった(実測 21px 対 45px)。縦の inset を持つのは TextField の写しから残った .sc-datefield-input で、実際に描かれる桁(.sc-datefield-segment)は padding-block: 0 だった。日付の欄を使う三部品すべてに伝播していた | 桁の行(control)へ縦の inset を移した。桁ごとに付けると押した桁の面が背丈いっぱいになって重く見えるので、行が持つ。四つとも 45px で揃った(TextField と同じ) | A(解消済み。size.md §2 の「高さは一次内容が決める」は満たしていたが、一次内容が inset を持っていなかった) |
| 125 | TimeField | 午前・午後を持つ欄だけ背が高い(実測 50px 対 45px)。二択の当たり判定(24px)を見た目の高さで取っていたため、桁の行より背が高くなっていた | 見た目の高さは桁の行に合わせ、当たり判定は擬似要素で欄の縦の余白まで広げる(見た目の大きさと当たり判定を別にするのは Slider のサムと同じ形。size.md §4)。実測で、見た目の箱の外(余白の中)を触っても選択が移る | B(見た目と当たり判定の分離。門は満たしたまま) |
| 126 | Select / Combobox | 仮想焦点の一覧を辿る計算(選べない候補を飛ばし、端で止め、開いたときの初手を決める)が Select に閉じており、Combobox が同じものを必要とした | internal/listbox.ts へ純粋な計算として抜き、両方が引く。焦点の移動と値の公開は各部品に残した(Combobox は打った文字も抱えるので、閉じ方が違う) | B(重複の解消。挙動は変えていない) |
| 127 | Combobox | 打った文字を捨てて戻すとき、戻す先を「いま出ている候補」から引いていた。絞り込みは器の外にあるので、選んだ相手が候補から消えた瞬間に戻す先を見失う。「向日葵デザイン」を選んでから合わない文字を打つと、値は残っているのに欄だけ空になった(Chromium で発覚) | 選んだ時点の表示を器が覚え、そこへ戻す。候補の並びに頼らない。Combobox.md へ「選んだ相手の名前は器が覚える」を還流 | A(解消済み。仕様へ還元) |
| 128 | Combobox | 打つたびに候補が入れ替わるのに、仮の焦点が前の位置に残っていた。さっき指していた場所に別の選択肢が来るので、見ていない相手を Enter が選ぶ | 文字を打った後は仮の焦点を外す(APG の manual selection)。開いた直後だけ、選ばれている選択肢に置く。絞り込みを外に出した以上、器は先頭が最も近い候補かどうかを知らないので、自動で選ぶ形は採れない。Combobox.md へ還流 | A(解消済み。仕様へ還元) |
| 129 | (横断) | 目から隠して耳へ残す一組の宣言(position:absolute から clip-path:inset(50%) まで)が、11 の部品に 14 回写し取られている。件数の告知、名前を隠す口、読み上げ専用の補足で同じものを書いている | 今回は Combobox にも写した。共有の宣言へ寄せる follow-up を別 PR で(部品ごとに CSS を持つ形なので、寄せ先の作り方も併せて決める) | A・既知(重複。別 PR) |
| 130 | (レイアウト原始) / Combobox | 狭い画面で欄に焦点が乗った瞬間、iOS がページごと拡大した(実機)。打つ文字は 16px を満たしていたので #102 とは別の経路で、原因はページが版面より広かったこと。打てる欄は既定の幅(input は 20 文字ぶん、約 250px)を持ち、包む器が縮まないとその幅がページの幅になる。layout.md §2 は「包む器も縮む」を規範として持っていたのに、Stack / Cluster / Grid / Switcher のどれも min-inline-size: 0 を持っていなかった(規範だけ先にあり、実装が追いついていなかった) | 四つの原始へ min-inline-size: 0 を入れた。Popover の包み(アンカーを包むだけの箱)にも入れた。12 画面すべてで、390px と 320px の版面に対してページが広がらないことを実測。layout.md §2 へ「打てる欄も既定の幅を持つ」と、iOS の拡大がこの経路でも起きることを還元 | A(解消済み。仕様へ還元) |
| 131 | Combobox | CSS を Select から丸ごと写して差分を末尾へ足していたため、二つの規則が同じ要素を争い、後から書いた側が横の inset を 0 にしていた(打った文字が枠に触れる。実機で指摘された)。native の <select> 経路のための規則も、押すだけのトリガーの規則も、Combobox には要らないまま残っていた。項目の名前と補足を並べる規則も、私が書いた印付けと名前が合っておらず、横に並んでいた | CSS を書き直した(写しではなく、この部品が持つものだけ)。名前と補足は縦に積む(横に並べると一行が長くなり、面が画面の幅を越える) | B(写しの後始末。写す判断そのものを見直す) |
| 132 | Select / Combobox | 候補の面の下限を content にしていたので、選択肢の文字の長さで面の幅が変わっていた。Select では一度きりで済むが、Combobox は打つたびに候補が入れ替わるので、面が伸び縮みして目で追っている位置が毎回動く(指摘 2026-07-28) | 下限もアンカー幅にし、中身では広げない。溢れる選択肢は面の中で折り返す(切り詰めない。同じ前置きを持つ選択肢が見分けられなくなる)。小さなトリガーに付く面(アイコンだけのボタンの Menu)は別で、そこは下限が content でよい。分かれ目は「アンカーが面の中身の幅を代表しているか」。overlay.md §5 と Select.md、Combobox.md へ還元 | A(解消済み。仕様へ還元) |
| 133 | (見本) / (レイアウト原始) | 打っても選んでも欄の幅が動いた(指摘 2026-07-28)。欄は器いっぱいに広がるので、幅を決めるのは器のはずが、その器が幅を中身で決める箱(shrink-to-fit)だった。欄と説明文を一つの箱へ入れ、横並びの中へ置いていたので、説明文の長さが箱の幅を決め、欄がそれに従っていた。値はサーバから来るので、画面の骨格がその日のデータで変わる | 見本の組み方を直し(欄は段の全幅を持つ器の中へ置く)、900px で 579px、390px で 342px と、打っても選んでも動かないことを実測した。部品側の欠陥ではないが、踏みやすい形なので layout.md §2 へ「欄の幅を中身で決めさせない」を還元 | A(解消済み。仕様へ還元) |
| 134 | Popover / Tooltip | 面の向きと残りの空きを版面(window.innerWidth / innerHeight)で測っていた。触点で鍵盤が出ると見える高さは半分近くまで減るのに版面は変わらないので、面は「下に空きがある」と判断して鍵盤の下へ開く。欄のすぐ下に出ているのに利用者からは何も見えない。拡大でも同じずれが出る | 見えている領域(visualViewport)で測る。領域そのものが動く場面(鍵盤の出入り・拡大)を購読して決め直す(window の resize では届かない)。overlay.md §5 へ「描画領域はいま見えている領域であって版面ではない」を還元。単位試験は visualViewport を差し替えて、同じ欄が鍵盤の有無で上下どちらへ開くかを見る | A(解消済み。仕様へ還元) |
