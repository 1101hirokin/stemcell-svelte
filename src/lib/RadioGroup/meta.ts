/** 既定値の単一の源。conformance が契約と照合する。 */
export const META = {
  props: {
    value: {},
    disabled: { default: false },
    invalid: { default: false },
    required: { default: false },
  },
} as const;
